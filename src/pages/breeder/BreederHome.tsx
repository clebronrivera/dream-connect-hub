// /breeder — placeholder. The real home (4 litter cards reading from
// breeder_litter_summary) lands in PR 4.

export default function BreederHome() {
  return (
    <div className="mx-auto max-w-screen-sm space-y-4 px-4 py-8">
      <h1 className="text-2xl font-bold">Litters</h1>
      <p className="text-sm text-muted-foreground">
        Litter cards land in the next step.
      </p>
    </div>
  );
}
