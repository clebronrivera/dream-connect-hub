/**
 * Customer-facing site only. Admin is unaffected.
 * When false, public header omits Shop + Breeds and the home hero hides the optional story CTA.
 * Routes remain in App.tsx; set true to show those links again.
 */
export const PUBLIC_SHOW_SHOP_AND_BREEDS_NAV = false;
